<div class="article">
    <div class="articleTitle">
        {{title}} <span class="saveButton"><button class="addModifyNotes" saved="false" title="{{title}}" link="{{link}}" id="{{_id}}">Notes</button></span>
    </div>
    <div class="articleLink">
        <a href="{{link}}" target="_blank">Read it at NYTimes.com</a>
    </div>
</div>